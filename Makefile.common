CHPLFLAGS=--fast --local --vectorize \
	  --replace-array-accesses-with-ref-temps

MISCFLAGS=--print-passes --print-commands --savec c_code

all: $(MAIN)

$(MAIN):
	echo Building $@
	chpl $(CHPLFLAGS) $(MISCFLAGS) -o $@ $(SRC) \
	  --main-module=$(MAIN) $(USERFLAGS)

clean:
	rm -rf $(MAIN) c_code/
